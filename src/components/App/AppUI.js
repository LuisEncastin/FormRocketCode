import React from 'react';
import { Template } from '../Template/Template'

// Aquí se pueden añadir más componentes y funcionalidades
function AppUI() {
    return (
      <React.Fragment>
        <Template/>
      </React.Fragment>
    );
  }

export { AppUI };