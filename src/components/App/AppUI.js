import React from 'react';
import { Template } from '../Template/Template'

function AppUI() {
    return (
      <React.Fragment>
        <Template/>
      </React.Fragment>
    );
  }

export { AppUI };